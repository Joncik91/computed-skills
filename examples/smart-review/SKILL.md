---
name: smart-review
description: Context-aware code review that adapts its strategy based on what changed. Analyzes git state, detects security-sensitive files, config changes, and architectural scope, then generates a targeted review prompt. Gets smarter across runs.
---

!`python3 ${CLAUDE_SKILL_DIR}/scripts/generate.py $ARGUMENTS`
