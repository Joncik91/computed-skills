---
name: self-improve
description: Captures corrections, errors, and capability gaps during sessions to enable continuous improvement. Triggers automatically when the user corrects the agent, a command fails, a capability is missing, or a better approach is discovered. Promotes recurring patterns to permanent memory.
---

!`python3 ${CLAUDE_SKILL_DIR}/scripts/generate.py $ARGUMENTS`
